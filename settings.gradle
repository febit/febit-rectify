rootProject.name = 'febit-rectify'

subModule 'rectify-core'
subModule 'rectify-flink'
subModule 'rectify-sqlline'

def subModule(String name) {
  include ':febit-' + name
  project(':febit-' + name).projectDir = file(name)
}
